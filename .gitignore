# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# CDK
infra/cdk.out/
infra/cdk.context.json
*.cdk.staging/

# Python wheel build output (sea/pyproject.toml -> sea/dist/*.whl for the
# Glue Python shell job's --extra-py-files; rebuild, don't commit the wheel)
sea/dist/
sea/build/

# Trained model binaries + per-run metrics (regenerate via ml/train.py)
ml/models/

# Node / frontend
node_modules/
frontend/dist/
frontend/build/
npm-debug.log*

# AWS / credentials
.aws-sam/
*.pem

# Env files
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store

# Data (avoid committing large downloaded datasets)
*.parquet
data/raw/
data/curated/
